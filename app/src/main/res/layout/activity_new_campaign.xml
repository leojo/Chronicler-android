<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".ui.NewCampaignActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/campaign_name_field"
        android:hint="Campaign Name"
        android:layout_above="@+id/confirm_campaign_button"
        android:layout_marginTop="10sp"
        android:layout_marginBottom="20sp"
        android:layout_alignRight="@+id/confirm_campaign_button"
        android:layout_alignEnd="@+id/confirm_campaign_button"
        android:layout_alignLeft="@+id/confirm_campaign_button"
        android:layout_alignStart="@+id/confirm_campaign_button"
        android:textAlignment="center"
        android:textSize="28sp" />

    <Button
        android:layout_width="221dp"
        android:layout_height="wrap_content"
        style="@style/LoginButtonStyle"
        android:text="Confirm"
        android:id="@+id/confirm_campaign_button"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
